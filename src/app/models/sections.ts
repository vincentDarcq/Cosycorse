import { Activite } from "./activite";
import { Lieu } from "./lieu";

export class Section {
    items: Array<any>;
    constructor(
        items: Array<any>
    ){
        this.items = items
    }
}